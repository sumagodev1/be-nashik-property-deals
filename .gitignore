node_modules/
.env
.env.local
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Uploaded images / documents. Served from the server's local disk
# (uploads/public, uploads/private) — that filesystem is the source of truth
# and these files must never enter the repo: they include private documents,
# and a tracked copy would be clobbered on every deploy.
uploads/
