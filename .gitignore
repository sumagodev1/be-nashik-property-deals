node_modules/
.env
.env.local
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Uploaded images / documents.
# Storage moved to S3 (2026-08-21) — the bucket is the source of truth and
# these files must never enter the repo. The folder is KEPT on disk on the
# server as a backup during the migration; this only stops Git tracking it.
uploads/
